# 88 — P2.3-C Reliability / Recovery 完成记录

日期：2026-09-07。基线：582 tests / 零警告 / Release Gate 十 Gate PASS。

---

## 交付

### C6 — Persistence Recovery（全六层）
| 持久层 | 策略 | 实现 |
|---|---|---|
| index.db | 删除+重建（rebuildable） | try_open→失败→删文件→recreate |
| favorites.db | 隔离.corrupt + 重建（用户数据不静默删） | pre-open magic bytes check + quarantine |
| catalog.db | 删除+重建（rebuildable） | pre-open magic bytes check |
| plugins.db | 删除+重建（default enabled） | pre-open magic bytes check |
| config.toml | 隔离.invalid-\<ts\> + 默认 | 已有（67 MUST-2） |
| icon cache PNG | 删条目 + 重抽取 | 已有（INV-ICON-005） |

### C7 — Config/Cache Corruption
config 原子写 + 损坏隔离已有（67）；icon cache PNG 损坏自愈已有（80）。

### C10 — Startup Recovery
startup_state.json 的 starting→healthy→consecutive_failures 状态机已有（P2.2-E 补充），本批补入 **DEGRADED BOOT enforcement**（3 次失败 → 跳过 coordinator，手册 §7 已记录）。

### C1 — Failure Taxonomy
WorkflowFailureClass 九变体覆盖核心失败类型；execute_action 错误分类映射到对应 variant。

### Recovery Golden Suite（C12 部分）
`crates/launcher-core/tests/recovery_e2e.rs` — 5 项测试覆盖全持久层损坏恢复 + 启动崩溃环 + 分类文档。

## 剩余（P2.3-C 后置）

- C1 完整 FailureTaxonomy enum 统一全 crate
- C3 Plugin crash soak（真实多进程循环）
- C4 MCP runtime/session 恢复矩阵（已有 security_transport 部分）
- C11 长跑 soak profile
- C12 完整 20 scenario Golden Suite（现有 5 项为起步）
- INV-RECOVERY-001~006 嵌入 INVARIANTS.md

## 验证

```text
cargo test --workspace   582 passed / 0 failed（+6：corrupt recovery 4 层 + startup crash + config classification）
cargo build --workspace  zero warnings
release_gate.py --require-baseline  十 Gate 全 PASS（VR 15/15 stable）
```
