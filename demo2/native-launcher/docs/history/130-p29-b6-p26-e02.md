# 130 — P2.9 Batch 6 完成 + P2.6 Batch 10：E02 宿主投影层

> 日期：2026-09-09。范围：P2.9 Batch 6（history/125）+ P2.6 E02 宿主侧
> Editor 投影层。输入基线：history/125（780 tests）。

## P2.9 Batch 6（已在 history/125 交付）

- Race/Security/Fault 全矩阵测试 + G16 入 gate；G01~G16 全 PASS。

## P2.6 E02 — Editor 宿主投影层（review 60 §58 ViewModel 模式）

- `apps/launcher-app/src/editor_surface.rs`（新）：
  `EditorSurface`（持有 GraphEditor）+ `EditorRow` DTO（node_id/
  action_ref/condition 可读形式）+ `refresh()` 投影 + 全操作路由
  （add/remove/edge/condition/undo/redo，每次变更刷新行）。
- **架构约束遵循**：launcher-ui 禁止依赖 launcher-workflow
  （UI_FORBIDDEN 守卫）——投影由宿主完成，Slint surface 只消费 DTO 行
  （E03-E05 接线时直接用 `EditorSurface.rows`）。
- E05 验证面前身：`invalid_draft_is_detectable` 测试证明宿主可对草稿态
  运行 §10 校验（画布上显示验证错误的数据源）。
- 测试 3 条：投影刷新（排序）、undo/redo 刷新、非法草稿可检测。

## Gate 结果

- `cargo test --workspace`：**790 passed / 0 failed**（784 → 790，+6 含
  P2.9 Batch 6 的 4 条）
- `cargo build --workspace`：零警告；`check_topology.py`：ok

## P2.6 剩余

E03-E05（Slint surface 消费 EditorRow 行 + 验证错误显示）、
F/G QA+Release 收口。
