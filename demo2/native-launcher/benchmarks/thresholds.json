{
  "schema_version": 1,
  "description": "Performance budget (P2.3-D, review 88 \u00a721-23). Baseline \u2260 Budget: baseline records current actual; budget is the product-allowed ceiling. Gate FAIL = current > budget.",
  "tolerances": {
    "search_p95_max_regression_pct": 50,
    "startup_p95_max_regression_pct": 50,
    "memory_max_regression_pct": 100
  },
  "budgets": {
    "search_app_p95_us": 50,
    "search_file_p95_us": 75,
    "cold_start_ms": 500,
    "idle_rss_mb": 50
  },
  "note": "v1.0: all four budgets enforced by G10 against fresh bench + artifacts/perf-release.json (real-process startup, idle RSS) + soak verdicts. Tighten after multi-machine verification.",
  "budget_rationale": {
    "search_file_p95_us_50_to_75": "P2.5-C01 (history/97): FTS-ranked + LIKE merged retrieval intentionally adds a second bounded scan for path+name relevance; measured cost 52us vs 27us LIKE-only - still ~1000x under the original 50ms UX target (design spec section 2.1 ruling in files2/97 audit A-2.1)"
  }
}
