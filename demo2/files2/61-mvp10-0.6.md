可以。**如果你的策略是先把 P1 完整做完，然后把产品拉到“基础 Launcher 真正可用”的状态，我赞成。P2 以后不需要全部做。**

但需要区分：

> **“必做”不是指产品永远不能缺，而是指为了让整个架构成为一个成熟的 Launcher + 可扩展平台，哪些能力最终值得做。**

我建议把 P2+ 分成 **核心必做、建议做、明确后置** 三档。

---

# 一、先给结论

按照你目前的架构与产品定位，我会这样排：

| 阶段/能力                               | 优先级 | 是否最终必做  | 说明                       |
| ----------------------------------- | --: | ------- | ------------------------ |
| **P2-A 基础搜索增强**                     |   S | **是**   | Launcher 的核心竞争力          |
| **P2-B Plugin Ecosystem**           |   S | **是**   | 决定可扩展性                   |
| **P2-C Windows 深度集成**               |   S | **是**   | 决定是否真正像 Windows Launcher |
| **P2-D Settings / Profiles / 管理能力** |   A | **基本是** | 产品化必需                    |
| **P2-E MCP**                        |   A | 否       | 扩展生态                     |
| **P2-F AI Planner 增强**              |   A | 否       | 已有基础，后续再做                |
| **P3 Workflow**                     |   B | 否       | 自动化平台能力                  |
| **P3 Agent**                        | B/C | 否       | 高级能力                     |
| **P3 Durable Runtime / State**      |   B | 否       | 复杂度高                     |
| **P4 Enterprise / Sync / Cloud**    |   C | 否       | 非核心                      |
| **P4 Marketplace**                  |   C | 否       | 生态成熟后再考虑                 |

因此：

> **P1 做完以后，完全可以停在 P2 的核心部分，而不用把 Workflow / Agent / 云同步 / Marketplace 等全部做完。**

---

# 二、不过有一个重要调整

你前面定义的 P1：

```text
P1-A Runtime Persistence
P1-B Persistent MCP / Plugin Session
P1-C Workflow v0.2
P1-D Agent Runtime
P1-E Visual Polish
```

全部完成之后，实际上已经不是普通 Launcher 了，而是：

```text
Launcher
+
Plugin Platform
+
Workflow Engine
+
AI Agent Runtime
```

所以 **P2 不应该继续无止境扩大功能**。

P2 更应该做：

> **“把 Launcher 核心打磨到稳定、快速、生态可用。”**

也就是说，P2 是**产品化与生态阶段**，不是继续堆新的大功能。

---

# 三、P2-A：搜索系统 —— 必做

这是我认为 P2 中最重要的一项。

因为你的 Launcher 最终真正的核心是：

```text
输入一句话
↓
快速找到正确东西
↓
正确排序
↓
立即 Action
```

所以必须进一步加强：

```text
Application Search
File Search
Folder Search
Recent
History
Keyword / Alias
Ranking
Fuzzy Search
Path Search
Metadata Search
```

最终应该做到：

```text
query
 ↓
candidate retrieval
 ↓
normalization
 ↓
ranking
 ↓
context-aware ranking
 ↓
result
```

### 为什么这是“必做”

因为即使：

```text
Agent
Workflow
MCP
Plugin
```

全部做完：

如果：

```text
"vsc"
```

搜不准 VS Code；

```text
"abc.pdf"
```

找不到文件；

```text
"download"
```

结果顺序很差；

那么这个产品仍然不是优秀 Launcher。

所以：

> **搜索是整个项目的长期主线，优先级最高。**

---

# 四、P2-B：Plugin Ecosystem —— 必做

你现在已经有 PluginBroker / Manifest / IPC / Runtime。

下一步真正值得做的是：

```text
插件开发体验
+
插件生态
```

例如：

```text
Plugin Manifest v1
Plugin SDK
Plugin Example
Plugin Lifecycle
Plugin Config
Plugin Permission Declaration
Plugin Update
Plugin Compatibility
Plugin Diagnostics
```

尤其是：

```text
插件如何开发
插件如何安装
插件如何升级
插件如何卸载
插件如何诊断
```

这些才决定插件体系能不能长期存在。

---

# 五、插件市场反而不是必做

我不建议一开始做：

```text
云端 Marketplace
账号
支付
插件商店
自动更新服务器
```

这些都是：

```text
生态商业化问题
```

不是技术核心。

第一阶段：

```text
local plugin directory
+
manifest
+
manual install
```

就够了。

---

# 六、P2-C：Windows 深度集成 —— 我认为是必做

你的产品是：

> Windows Launcher

那么最终必须真正理解 Windows。

建议逐渐加入：

```text
Start Menu
AppUserModelID
UWP / MSIX
Installed Apps
Windows Settings
Control Panel
Windows URI
Shell verbs
Jump List
File associations
Windows Search integration
```

以及：

```text
open
open containing folder
copy path
properties
run as admin
uninstall
```

甚至后续：

```text
Shell Context Action
```

这样才能让：

```text
Explorer
Start Menu
Run
Wox
Flow Launcher
```

这些功能逐渐被你的 Launcher 统一。

---

# 七、P2-D：History / Recent —— 基本必做

Launcher 使用体验非常依赖：

```text
History
Recent
Favorites
Frequency
Recency
```

例如：

```text
chrome
vscode
Downloads
foo.py
Git Bash
```

用户连续使用几次后：

```text
typing "ch"
```

Chrome 自动排前面。

这会显著提升产品“越用越顺”的感觉。

---

# 八、Context-aware Ranking —— 推荐做

你已有：

```text
launcher-context
```

后面应该逐步利用。

例如：

```text
当前文件夹：
D:\Projects\Foo
```

搜索：

```text
git
```

那么：

```text
Git Bash
Git
git.exe
Git extensions
```

的排序可以根据上下文改变。

又比如：

```text
当前选中了 PDF
```

搜索：

```text
open
```

Action 可以更偏：

```text
Open
Open with
Compress
Copy path
```

这是 Launcher 真正从：

```text
搜索框
```

进化到：

```text
Contextual launcher
```

的关键。

---

# 九、P2-E：MCP —— 推荐但不是必做

这里我会把之前的判断明确下来：

MCP 对你的项目有价值，但它不是：

```text
Launcher Core
```

而是：

```text
External Tool Ecosystem
```

因此：

```text
Plugin
    = 核心扩展机制

MCP
    = 外部工具生态接入
```

你现在已经提前把 MCP 架构打得非常完整，所以后面完全可以：

```text
P2
MCP production hardening
```

但如果项目资源有限：

> **MCP 可以整个冻结，不影响 Launcher 作为产品存在。**

---

# 十、P2-F：AI Planner —— 不属于必做

你已经做了：

```text
LLMProvider
LLMPlanner
ActionCatalog
ActionProposal
```

这已经足以成为：

```text
AI extension point
```

之后真正值得做的只是：

```text
better prompt
better ranking
better catalog
better local model integration
```

但：

```text
AI
```

本身不是 Launcher 的必要能力。

因此：

> **AI 是差异化功能，不是基础产品依赖。**

---

# 十一、Workflow —— 非必做

即使 P1-C 已经做完，我依然建议：

```text
Workflow v0.2
```

作为稳定高级功能存在即可。

没有必要继续无限增加：

```text
loop
parallel
variables v2
subworkflow
scheduler
distributed workflow
```

除非之后你确认：

> 用户真的需要把 Launcher 当自动化工具使用。

否则 Workflow 到：

```text
Variables
Conditions
Branch
Retry
Confirmation
```

就已经足够。

---

# 十二、Agent —— 更不是必做

这一点我会比之前更明确：

> **Agent 不属于 Launcher 的必要能力。**

它是：

```text
高级 AI Automation
```

如果将来用户主要用：

```text
文件搜索
应用启动
Action
插件
```

那么 Agent 可以永远停留在：

```text
实验性 / optional
```

而不会损害核心产品。

---

# 十三、真正可能成为“后期必做”的其实是搜索、插件、系统集成

换句话说，P2/P3 不应该按照：

```text
功能越来越炫
```

来规划，而应该：

```text
Launcher Core
    ↓
Extension Platform
    ↓
Windows Integration
    ↓
Optional AI
    ↓
Optional Automation
```

---

# 十四、我建议重新定义 P2

比起：

```text
P2 = 更多功能
```

我建议：

# P2 — Launcher 1.0 Foundation

目标：

> **把当前架构变成稳定、快速、长期可用的日常 Launcher。**

包括：

```text
P2-A
Search Engine 2.0

P2-B
Application Index 2.0

P2-C
File Index 2.0

P2-D
Ranking / History / Recent

P2-E
Action Expansion

P2-F
Plugin Ecosystem

P2-G
Windows Integration

P2-H
Settings / Profiles / Diagnostics

P2-I
Performance / Memory / Stability

P2-J
Packaging / Installer / Upgrade
```

这个阶段完成以后，我认为就应该打：

```text
Launcher 1.0
```

---

# 十五、尤其要做 Installer / Upgrade

这项经常被技术设计忽略，但对于真正可用的软件非常重要。

至少：

```text
安装
卸载
升级
保留配置
保留索引
插件兼容
崩溃恢复
自启动
开机启动
```

还需要处理：

```text
single instance
startup registration
data directory
config migration
```

这才真正从：

```text
开发项目
```

进入：

```text
桌面软件
```

---

# 十六、P3 可以大量砍掉

P3 我建议只保留：

```text
Advanced AI
Advanced Workflow
Agent
```

而下面这些全部不是必需：

```text
Cloud sync
Account
Marketplace
Distributed execution
Team collaboration
Enterprise management
Telemetry platform
Remote agent
```

除非后面产品方向发生变化。

---

# 十七、推荐最终路线

我现在更建议把整个项目重新收敛成：

```text
P0
Architecture Foundation
✅

P1
Advanced Runtime + Workflow + Agent + UI
↓
完成后形成完整技术底座
✅

P2
Launcher 1.0
★★★★★★★★★★
核心必做

    Search
    App
    Files
    Ranking
    History
    Actions
    Plugins
    Windows
    Settings
    Performance
    Packaging

P3
Optional Extensions
★★★★

    MCP
    Advanced AI
    Advanced Workflow
    Agent improvements

P4
Experimental / Ecosystem
★★

    Marketplace
    Cloud
    Sync
    Team
    Enterprise
```

---

# 十八、如果你坚持“P1 全部完成”，这个顺序反而很好

因为 P1 完成后，你会拥有一个非常完整的底层能力：

```text
                  P1 Complete
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
     Runtime        Workflow         Agent
        │              │              │
        └──────────────┼──────────────┘
                       ▼
                  ActionEngine
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
      Plugin           MCP            System
```

此时不要再继续造基础设施。

应该回头：

```text
                    P2
                     │
          ┌──────────┼──────────┐
          ▼          ▼          ▼
       Search      Plugin     Windows
          │          │          │
          └──────────┼──────────┘
                     ▼
                  Launcher
                     1.0
```

---

# 十九、我会把“是否必做”最终压缩成这张表

| 能力                                    | 最终建议  |
| ------------------------------------- | ----- |
| **搜索引擎**                              | 🔴 必做 |
| **App/File/Folder Index**             | 🔴 必做 |
| **Ranking / Fuzzy / History**         | 🔴 必做 |
| **Action**                            | 🔴 必做 |
| **基础 Plugin**                         | 🔴 必做 |
| **Plugin SDK / Manifest / Lifecycle** | 🔴 必做 |
| **Windows Integration**               | 🔴 必做 |
| **Settings / Config Migration**       | 🔴 必做 |
| **Performance / Memory / Crash**      | 🔴 必做 |
| **Installer / Upgrade**               | 🔴 必做 |
| MCP                                   | 🟠 推荐 |
| AI Planner                            | 🟠 推荐 |
| Workflow                              | 🟡 可选 |
| Agent                                 | 🟡 可选 |
| Durable Agent                         | ⚪ 后置  |
| Cloud Sync                            | ⚪ 后置  |
| Marketplace                           | ⚪ 后置  |
| Enterprise                            | ⚪ 后置  |

---

## 最终建议

**你可以放心地按现在的计划把 P1 全部完成，然后停下来，不必继续无限扩张。**

P1 完成后，我建议不要直接进入“P2 AI/MCP/Agent 大扩展”，而是把 **P2 定义成 Launcher 1.0 产品化阶段**：

```text
P1
技术能力完成
        ↓
P2
搜索 + 应用 + 文件 + Action + Plugin + Windows
        ↓
Launcher 1.0
        ↓
根据真实使用反馈决定
        ├── MCP
        ├── AI
        ├── Workflow
        └── Agent
```

其中真正具有**长期不可替代性**的是：

> **Search → Ranking → Action → Plugin → Windows Integration**

这五个才是这个项目的“核心产品壁垒”；AI、Workflow、Agent 更适合作为建立在它们之上的可选增强层。
