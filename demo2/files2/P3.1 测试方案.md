# P3.1 测试方案 — Plugin Window & Management 1.0

**状态：FROZEN TEST PLAN**

---

# 1. B0 管理窗口

| # | 层 | 内容 |
|---|---|---|
| MGT-1 | 单测 | 工作流列表读取 workflows 目录（存在/空/损坏 JSON 忽略） |
| MGT-2 | 单测 | settings 复用项：apply("theme"/"autostart") roundtrip（复用 P3.0 SET 系列） |
| MGT-3 | 单测 | 插件启停即时生效：registry.set_enabled(false) 后 **provider.query 立即为空**（不再等重启）——B0 核心回归 |
| MGT-4 | 单测 | 启停持久化：set_enabled 后重开 registry 状态保持（既有行为回归） |
| MGT-5 | 集成 | 管理窗口实例化：四 Tab 切换、插件模型绑定（row_data 断言） |
| MGT-6 | E2E（手工脚本） | 搜索"管理"打开窗口 → 禁用 echo 插件 → 弹窗搜索该插件命令消失 → 重新启用恢复 |
| MGT-7 | E2E（手工脚本） | 工作流删除后 workflows 目录文件消失；编辑入口打开编辑器 |
| MGT-8 | 回归 | 弹窗三 Mode / VR 基线不受影响（既有 15+15 张 diff 干净） |

# 2. B1 窗口发现与置顶

| # | 层 | 内容 |
|---|---|---|
| WIN-1 | 单测 | `pid()`：spawn 一个 echo 进程后 pid 非零且进程存在 |
| WIN-2 | 单测 | 窗口枚举过滤：自身窗口 / 无标题窗口 / 非目标 pid 窗口被排除 |
| WIN-3 | 手工 | manifest 未声明 `window.ui` 的插件不出现在枚举列表 |
| WIN-4 | 手工 | 置顶：插件窗口（示例 tkinter fixture）置顶后压在记事本之上；再切换取消置顶 |
| WIN-5 | 安全 | 提权目标（以管理员运行的记事本）置顶跟随 → 明确报错（UIPI） |
| WIN-6 | 回归 | Job Object 隔离/协议测试不受 pid 暴露影响（plugin-host 全套） |

# 3. B2 跟随钉

| # | 层 | 内容 |
|---|---|---|
| FOL-1 | 单测 | 跟随状态机：set target → 前台变化事件 → INSERTAFTER 调用参数正确（mock Win32 边界） |
| FOL-2 | 手工 | 目标 = 记事本：切到记事本插件窗在上方；切到浏览器插件窗不在上方 |
| FOL-3 | 手工 | 目标销毁（关闭记事本）→ 自动解除 + 状态提示 |
| FOL-4 | 手工 | 插件进程退出 → 跟随自动清理，无悬挂句柄操作 |
| FOL-5 | 资源 | WinEventHook 线程空闲 0 CPU（采样 30s） |

# 4. B3 桌面钉（实验性）

| # | 内容 |
|---|---|
| DESK-1 | WorkerW 生成/回退路径（0x052C 不生效时的 Enum 兜底） |
| DESK-2 | 框架矩阵：tkinter / Electron / Qt 窗口挂载后渲染+交互正常（逐项记录） |
| DESK-3 | 解除钉住恢复原父（SetParent NULL + 还原样式位） |

# 5. 性能/回归预算（每批 Gate）

| # | 内容 | 预算 |
|---|---|---|
| PERF | 管理窗口打开 | < 300ms（模型缓存 < 100ms） |
| PERF | 插件 query 增量（registry 读） | < 1ms/次 |
| REG | `cargo test --workspace` 全绿 + 零警告 + topology ok | 每批 |
| REG | 弹窗 VR（dark+light 30 张）diff 干净 | B0 起 |
